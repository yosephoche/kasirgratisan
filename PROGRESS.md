# PROGRESS.md — FRONTEND (fork kasirgratisan)

> Status: `[ ]` TODO · `[~]` WIP · `[x]` DONE · `[!]` BLOCKED. Detail & Acceptance
> ada di `PLAN.md`. Repo ini deploy ke Vercel.

**Fase aktif:** Fase E — deploy Vercel + backend VPS SELESAI & Acceptance TERVERIFIKASI 2026-08-09 (`kasirgratisan-murex.vercel.app` ↔ `api-kasir.paradox.web.id`). Rebrand nama/logo/watermark masih **ditunda** (belum ada aset final) — sisanya Fase E secara fungsional selesai.
Ad-hoc di luar Fase A-H: **fitur invite-link karyawan** — frontend selesai & di-push (`7c05d29`), backend endpoint sudah live di VPS (`api-kasir.paradox.web.id`, smoke-test `POST /api/invites/:token/redeem` → 404 "Undangan tidak ditemukan" sesuai kontrak). Belum diverifikasi end-to-end (generate link asli → redeem → login karyawan) — lihat Log.
**Terakhir diperbarui:** 2026-08-12

---

## Urutan prioritas (frontend)
1. Fase H (resep + overhead) — independen, tanpa backend
2. Fase D sisi client (pull-sync) — butuh backend Fase C/D
3. Fase E sisi frontend (de-brand + Vercel)

---

## Fase H — Resep / BOM + Overhead + Master Data Material  `[x]` (Acceptance terverifikasi 2026-08-07)
- [x] H.1 Dexie v15 + `ProductRecipe` + `OverheadConfig` + field override di `Product` + daftar `syncTables`
- [x] H.2 Konvensi produk (bahan `trackStock:true`, produk jadi `trackStock:false`)
- [x] H.3 `RecipeEditor.tsx` + input override overhead di `Products.tsx`
- [x] H.4 Patch checkout resep-aware `Cashier.tsx` (+ balik di refund/void, `TransactionHistory.tsx`)
- [x] H.5 `getAvailableQty` (ketersediaan = min bahan) di `src/lib/recipe.ts`
- [x] H.6 `OverheadSettings.tsx` (Workspace) + route `/settings/overhead` di `App.tsx` + menu di `Settings.tsx`
- [x] H.7 HPP final (`src/lib/overhead.ts`) + snapshot ke `transactionItems`
- [x] H.8 Laporan rekonsiliasi overhead di `Reports.tsx`
- [x] H.9 Tambah `productRecipes` & `overheadConfig` ke push/pull payload di `sync.ts`
- [x] i18n id/en/ms untuk semua string baru
- [x] H.10 Revisi: Dexie v16 — tabel `Material` baru, `StockIn`/`HppHistory` productId→optional+materialId, `ProductRecipe.ingredientProductId`→`ingredientMaterialId` (clean cutover)
- [x] H.11 `Materials.tsx` (CRUD master data bahan/packaging) + route `/materials` + menu di `Settings.tsx`
- [x] H.12 `MaterialPicker.tsx`
- [x] H.13 `StockIn.tsx` — toggle Produk/Material, weighted-average cost untuk keduanya
- [x] H.14 `RecipeEditor.tsx` pakai `MaterialPicker` + `ingredientMaterialId`
- [x] H.15 Rework `src/lib/recipe.ts` (`RecipeCalcContext`, 3 fungsi inti baca dari `materials`)
- [x] H.16 Rewire `Cashier.tsx`/`TransactionHistory.tsx` + `sync.ts`/`cloud-api.ts` + update `PLAN.md` §6b
- [x] **Acceptance H terpenuhi** (termasuk Acceptance tambahan §6b PLAN.md) — diverifikasi manual via browser 2026-08-07

## Fase D (sisi client) — Pull Sync  `[x]` (Acceptance terverifikasi 2026-08-08)
- [x] Tambah `lastPulledAt` (storeSettings) + `pullSyncData()` di `src/lib/sync.ts`
- [x] Merge ke Dexie (bandingkan `updatedAt`, terapkan tombstone)
- [x] Panggil pull di startup + setelah push + trigger (polling 60 detik di `App.tsx`)
- [x] test `sync-pull.test.ts`
- [x] **Acceptance D (sisi client)** — perubahan device A muncul di device B — TERVERIFIKASI end-to-end lawan backend asli (lihat Log)

## Fase E (sisi frontend) — De-brand + Vercel  `[~]`
- [x] Matikan telemetri: `version-check.ts` (fetch ke `api.kasirgratisan.my.id` di-no-op — endpoint pihak asli, bukan milik sendiri). `analytics.ts`/`onesignal.ts` sudah ter-gate `VITE_GA_MEASUREMENT_ID`/`VITE_ONESIGNAL_APP_ID` sejak awal (kosong = no-op), tidak perlu ubah kode — cukup pastikan env kosong di Vercel.
- [ ] Rebrand watermark struk (`printer.ts`/`Receipt.tsx`/`KitchenTicket.tsx`) + nama/logo (`index.html`, `public/`, `capacitor.config.ts`, i18n) — **DITUNDA**, user belum punya nama/aset final.
- [x] `.env.example` diberi catatan `VITE_AUTH_API_URL` produksi harus arah ke backend ter-deploy (bukan localhost). `VITE_GOOGLE_CLIENT_ID` sudah ada dari sesi Fase A.
- [x] `vercel.json` (SPA rewrite `/(.*) → /index.html`, standar Vite SPA di Vercel)
- [x] Deploy Vercel (`kasirgratisan-murex.vercel.app`, manual oleh user) + backend VPS (`api-kasir.paradox.web.id`, manual oleh user)
- [x] **Acceptance E (frontend)** — TERVERIFIKASI 2026-08-09 (lihat Log): tanpa telemetri asli, app jalan di domain Vercel, login+store+sync jalan lintas domain lawan backend produksi. Custom domain sendiri belum diset (masih domain default Vercel) — opsional, tidak menghambat Acceptance.

---

## Log
- _(tanggal)_ — inisialisasi tracker frontend.
- 2026-08-07 — H.1 selesai: `src/lib/db.ts` naik ke v15, tambah interface `ProductRecipe` & `OverheadConfig`, tabel baru `productRecipes`/`overheadConfig` didaftarkan di kelas Dexie + `syncTables` + `sanitizeDatabaseDates`. Field `overheadCost`/`overheadMode` (opsional) ditambah di `Product`. `tsc --noEmit` bersih. Lanjut H.2.
- 2026-08-07 — H.2 selesai: no-op verifikasi. `Products.tsx` sudah punya toggle `trackStock` (Switch, baris ~796) yang menggerbang UI stok — konvensi bahan=`true`/produk jadi=`false` sudah didukung penuh oleh kode existing, tidak perlu perubahan. Lanjut H.3 (RecipeEditor.tsx + input override overhead).
- 2026-08-07 — H.3 selesai: `src/components/RecipeEditor.tsx` baru (CRUD baris `productRecipes` via `useLiveQuery`, pilih bahan lewat `ProductPicker` yang difilter `isStockManaged`, takaran via `NumberInput` decimal). `Products.tsx`: tambah section override overhead (`overheadCost`/`overheadMode`, `NumberInput` + `Select`) dan render `RecipeEditor` di dialog edit (hanya saat `editProduct?.id` ada — produk baru harus disimpan dulu, ada hint `recipe.saveFirstHint`). i18n id/en/ms lengkap (`dialog.overhead*`, namespace `recipe.*`). `tsc --noEmit` bersih.
  - **Catatan scope:** preview HPP real-time (biayaBahan+overhead) yang disebut di §H.3 PLAN.md **belum** diimplementasi — butuh `computeBiayaBahan`/`resolveOverhead` dari H.5–H.7 (`src/lib/recipe.ts`, `src/lib/overhead.ts`) yang belum dibuat. Sengaja ditunda agar tidak lompat fase (CLAUDE.md §3). Akan ditambahkan saat H.7.
  - Lanjut H.4 (patch checkout resep-aware di `Cashier.tsx`).
- 2026-08-07 — H.4 selesai: `src/lib/recipe.ts` baru dengan `applyRecipeAwareStockDelta(productId, deltaQty)` — produk tanpa resep aktif kurangi/kembalikan stok sendiri flat 1:1 (perilaku lama, gated `isStockManaged`); produk ber-resep kurangi/kembalikan stok tiap bahan sesuai takaran × deltaQty (gated `isStockManaged` per bahan). Dipakai di 6 titik `Cashier.tsx` (saveOpenBill baru/update/restore-removed, cancelOpenBill, checkout baru/update-existing-bill) & 1 titik `TransactionHistory.tsx` (hapus transaksi + restoreStock) — semua dibungkus `db.transaction('rw', db.products, db.productRecipes, ...)` sesuai PLAN.md. `tsc --noEmit` bersih.
  - Lanjut H.5 (`getAvailableQty` di `src/lib/recipe.ts` + badge stok/blokir jual di `Cashier.tsx`).
- 2026-08-07 — H.5 selesai: `src/lib/recipe.ts` tambah `buildAvailableQtyContext(products, recipes)` + `getAvailableQty(product, ctx)` — produk ber-resep = min(floor(stok_bahan/takaran)) atas bahan yang dikelola stoknya (Infinity bila tak ada pembatas); produk tanpa resep = stok sendiri (Infinity bila trackStock:false, perilaku lama). `Cashier.tsx`: `availCtx` dibangun via `useMemo` dari `useLiveQuery` produk+resep; ganti seluruh pemakaian `isStockManaged(p)+p.stock` jadi `getAvailableQty` di 8 titik — filter daftar produk, `addToCart`, `updateQty`, 2× scan handler, 2× badge stok kartu produk (grid & rows), 2× klem qty manual di keranjang. Import `isStockManaged` yang jadi tak terpakai di `Cashier.tsx` dihapus. `tsc --noEmit` bersih.
  - Lanjut H.6 (`OverheadSettings.tsx` Workspace + route `/settings/overhead`).
- 2026-08-07 — H.6 selesai: `src/lib/overhead.ts` baru (`computeOverheadPerUnit(config)` — rumus predetermined overhead rate §H.6). `src/pages/settings/OverheadSettings.tsx` baru (form 9 komponen biaya + hasil `overheadPerUnit` real-time, pola sama `ReceiptSettings.tsx`, singleton row `overheadConfig`, gated `can('manage_store_settings')`). Route `/settings/overhead` didaftarkan di `App.tsx`, menu card ditambah di `Settings.tsx` (ikon `Calculator`). i18n id/en/ms lengkap (`masterData.overhead.*`, `overheadSettings.*`).
  - **Catatan infra penting:** `node_modules/` ternyata belum pernah ter-install di repo ini, dan shell default pakai Node v14.15.0 (via nvm) yang **tidak kompatibel** dengan toolchain repo (Vite 6, typescript-eslint 8, dst butuh Node ≥18). Akibatnya semua `tsc --noEmit` di sesi ini sejak H.1 **silent no-op** (npx menjalankan placeholder package "tsc" kosong, bukan compiler asli — selalu "sukses" walau ada error). Sudah diperbaiki: `npm install` dijalankan di bawah `nvm use 20.20.0`, lalu re-verifikasi pakai `./node_modules/.bin/tsc` langsung (bukan `npx tsc`) — **hasil bersih, tidak ada regresi dari H.1–H.6**. Vite dev server juga dites jalan & serve semua file baru tanpa error transform. **Wajib untuk sesi berikutnya:** jalankan `nvm use 20.20.0` (atau versi ≥18 manapun) sebelum `npm install`/`tsc`/`vite`; jangan percaya `npx tsc` mentah-mentah, selalu panggil `./node_modules/.bin/tsc` langsung.
  - Browser Chrome extension tidak terhubung di sesi ini → tidak sempat klik-uji UI Overhead Workspace secara visual; hanya diverifikasi via tsc asli + Vite transform (200 OK, tanpa error esbuild).
  - Lanjut H.7 (HPP final `src/lib/overhead.ts` `resolveOverhead()` + `src/lib/recipe.ts` `computeBiayaBahan()`/`computeHppFinal()` + snapshot ke `transactionItems`).
- 2026-08-07 — H.7 selesai: `src/lib/recipe.ts` tambah `computeBiayaBahan(product, ctx)` (Σ HPP bahan × takaran; produk tanpa resep = `product.hpp`) & `computeHppFinal(product, ctx, overheadPerUnit)` (biayaBahan + overhead resolved). `src/lib/overhead.ts` tambah `resolveOverhead(product, biayaBahan, overheadPerUnit)` (override fixed/percent per produk, else default global). `Cashier.tsx`: `overheadConfig` di-load via `useLiveQuery`, `overheadPerUnit` via `computeOverheadPerUnit`, helper lokal `hppFinal(p)` menggantikan seluruh `item.product.hpp`/`c.product.hpp` mentah — dipakai di `totalProfit` & 4 titik snapshot `hpp` di `transactionItems` (saveOpenBill baru/update, checkout baru/update). Snapshot HPP historis otomatis stabil karena hanya jalur tulis yang diubah — `Reports.tsx`/`TransactionHistory.tsx` tetap baca field `hpp`/`profit` tersimpan, tidak dihitung ulang.
  - Verifikasi: real `./node_modules/.bin/tsc --noEmit` bersih. `package-lock.json` sempat ter-regenerasi besar-besaran (efek samping `npm install` di sesi lalu, format npm6→npm10) — sudah di-revert (`git checkout -- package-lock.json`), tidak disertakan.
  - Fase H tersisa: H.8 (rekonsiliasi overhead di Reports.tsx), H.9 (daftarkan `productRecipes`/`overheadConfig` ke `SyncPayload`/`cloud-api.ts`/push-pull), i18n final check.
- 2026-08-07 — H.8 selesai: `src/lib/overhead.ts` refactor — ekstrak `computeTotalOverheadPerMonth(config)` (dipakai ulang oleh `computeOverheadPerUnit`, single source of truth rumus total biaya overhead bulanan). `Reports.tsx`: query bulan kalender berjalan terpisah dari filter periode existing (`monthTransactions`/`monthTxItems`, basis §H.8 memang bulanan bukan daily/7/30), hitung `unitsSoldThisMonth`, `overheadAbsorbed = overheadPerUnit × unitsSoldThisMonth`, `totalOverheadActual`, `overheadVariance = actual − absorbed` (>0 under-absorbed/merah, <0 over-absorbed/hijau). Card baru "Rekonsiliasi Overhead (Bulan Ini)" tampil hanya bila `overheadConfig` sudah diisi. i18n id/en/ms lengkap (`overheadReconciliation.*`). Real `tsc --noEmit` bersih, `package-lock.json` tidak tersentuh.
  - Fase H tersisa: H.9 (daftarkan `productRecipes`/`overheadConfig` ke `SyncPayload`/`cloud-api.ts`/push-pull sync), lalu i18n final check + jalankan seluruh Acceptance H.
- 2026-08-07 — H.9 selesai: `src/lib/cloud-api.ts` `SyncPayload` tambah `productRecipes?`/`overheadConfig?`. `src/lib/sync.ts` `pushSyncData()`: kedua tabel ditambah ke `getDirtyRecords`, hitungan `totalDirtyCount`, payload push, dan loop update `syncedAt` pasca-push (dibungkus `db.transaction('rw', [...])` yang sudah ada). Tabel sudah terdaftar di `syncTables` sejak H.1 jadi dirty-tracking (`updatedAt`/`syncedAt` reset) otomatis aktif. `pullSyncData()` **belum ada** di repo ini (Fase D terpisah, belum dikerjakan) — akan otomatis mencakup kedua tabel baru saat Fase D digarap nanti, tidak ada tindakan lagi untuk sesi ini.
  - i18n final check: scan seluruh key baru Fase H (`recipe.*`/`dialog.overhead*` di products, `overheadSettings.*`/`masterData.overhead` di settings, `overheadReconciliation.*` di reports) — lengkap di id/en/ms. Ada gap i18n MS pre-existing besar (transactionHistory, supplier, employees, dll — ratusan key) yang **tidak terkait Fase H**, dibiarkan (di luar scope).
  - Real `tsc --noEmit` bersih. `package-lock.json` tidak tersentuh.
  - **Keputusan penting (catat untuk backend, sesuai CLAUDE.md §5):** bentuk `SyncPayload` di `cloud-api.ts` bertambah 2 field opsional (`productRecipes`, `overheadConfig`). User perlu menyalin ulang `cloud-api.ts` ke `backend/reference/` dan menambah 2 tabel + model Pydantic yang sesuai (`ProductRecipe`, `OverheadConfig`, field per §H.1 PLAN.md) di server saat backend akhirnya menerima payload ini — kalau tidak, field tambahan cukup diabaikan server (tidak breaking), tapi data resep/overhead tidak akan ke-backup ke cloud sampai backend diupdate.
  - **Fase H (H.1–H.9) SELESAI.** Acceptance H perlu diverifikasi manual oleh user di browser — sesi ini tidak bisa uji visual karena Chrome extension tidak terhubung.
- 2026-08-07 — **Revisi Fase H (H.10–H.16):** user menandai celah desain — bahan resep (H.1–H.9) salah pakai `products` sebagai sumber "bahan baku" (`trackStock:true` convention). Semestinya master data terpisah `Material` (bahan baku + packaging), tidak dicampur dengan produk yang dijual. Dikerjakan via `/plan` (lihat `~/.claude/plans/ada-1-hal-yang-prancy-lerdorf.md`), disetujui user dengan 2 keputusan eksplisit: (1) costing material reuse `StockIn.tsx` existing (bukan halaman terpisah) dengan toggle Produk/Material; (2) clean cutover untuk `ProductRecipe.ingredientProductId`→`ingredientMaterialId` (rename, bukan migrasi — resep lama di-soft-delete otomatis, deliberate exception dari aturan aditif, disetujui user karena fitur belum dipakai data riil).
  - **H.10:** `src/lib/db.ts` naik ke v16. Interface `Material` baru. `StockIn.productId`/`HppHistory.productId` dilonggarkan jadi optional + `materialId?` baru (tepat salah satu terisi per baris). `ProductRecipe.ingredientProductId` di-rename `ingredientMaterialId`. `.upgrade()` v16 soft-delete semua `productRecipes` existing. `materials` didaftarkan ke `syncTables` & `sanitizeDatabaseDates`.
  - **H.11:** `src/pages/Materials.tsx` baru — CRUD (pola sama `Supplier.tsx`), filter tipe ingredient/packaging via `Tabs`, satuan reuse `SearchableSelect` dari `db.units`. Route `/materials` di `App.tsx`, menu card di `Settings.tsx` (ikon `Boxes`, gated `manage_stock_inout`). i18n id/en/ms (`material.*`, `transactionsAndStock.material`).
  - **H.12:** `src/components/MaterialPicker.tsx` baru — mirror `ProductPicker.tsx`, sumber `db.materials`.
  - **H.13:** `StockIn.tsx` — tambah toggle "Jenis: Produk/Material" (filter list + form dialog), formula weighted-average yang sama diterapkan ke `db.materials`+`hppHistory.materialId` saat mode Material. i18n `stockIn.typeFilter.*`/`dialog.materialLabel` (id/en saja — `stockIn`/`supplier` standalone block memang tidak ada di ms/settings.json, pre-existing gap, di luar scope).
  - **H.14:** `RecipeEditor.tsx` — `ProductPicker`→`MaterialPicker`, `ingredientProductId`→`ingredientMaterialId`, hapus filter `isStockManaged` (material selalu tracked), tambah hint `recipe.noMaterialsHint` saat belum ada material.
  - **H.15:** `src/lib/recipe.ts` rework total — `AvailableQtyContext`→`RecipeCalcContext` (+`materialsById`), `buildAvailableQtyContext`→`buildRecipeCalcContext(recipes, materials)`, `getAvailableQty`/`computeBiayaBahan`/`applyRecipeAwareStockDelta` semua baca `materials` bukan `products` untuk sisi bahan. Path produk-tanpa-resep (flat 1:1 pada `db.products`) tidak berubah.
  - **H.16:** `Cashier.tsx`/`TransactionHistory.tsx` — `materials` di-load via `useLiveQuery`, masuk ke `buildRecipeCalcContext`, `db.materials` ditambah ke 6 titik `db.transaction('rw', ...)`. `sync.ts`/`cloud-api.ts` — `materials` masuk `SyncPayload`+push (pola sama H.9). `PLAN.md` §6b baru ditambahkan (mendokumentasikan revisi tanpa menghapus teks H.1–H.9 lama).
  - **Temuan tooling kritis (koreksi memory `project-node-version-tsc-trap`):** bahkan `./node_modules/.bin/tsc --noEmit -p .` (yang dikira "real tsc" sejak H.6) ternyata **silent no-op** — `tsconfig.json` root cuma solution/references file (`"files": []`), `-p .` tanpa `--build` cek 0 file, selalu exit 0. Artinya SEMUA verifikasi tsc H.1–H.9 sebelumnya **palsu juga**. Perbaikan: pakai `./node_modules/.bin/tsc --noEmit -p tsconfig.app.json` (target langsung `include:["src"]`). Dibuktikan dengan baseline: stash tracked+untracked (git stash tidak stash file baru tanpa `-u`, dan bahkan dengan `-u` perlu diverifikasi manual via `ls`/`git status` — pindahkan file baru manual ke luar repo untuk baseline yang benar) → HEAD punya **20 error pre-existing** (Onboarding.tsx, use-auth.tsx, db.ts:887/917, file-utils.ts, onesignal.ts, Dashboard.tsx, Reports.tsx, 3 file test) tidak terkait Fase H sama sekali. Setelah H.10–H.16 selesai: **error set identik 100% dengan baseline** — nol regresi dari seluruh pekerjaan Fase H (H.1–H.16).
  - Chrome extension tidak terhubung sesi ini → belum sempat klik-uji visual (Materials CRUD, Stock In toggle, RecipeEditor pakai Material, checkout kurangi stok material).
- 2026-08-07 — **Verifikasi manual browser (Acceptance H) — SEMUA PASS**, extension Chrome berhasil connect setelah beberapa kali retry. Skenario diuji end-to-end di data toko baru (sample data seeded):
  1. Materials CRUD: buat "Gula Aren" (ingredient, 500 gram) & "Gelas 12oz" (packaging, 50 pcs) — badge tipe & filter tab (Semua/Bahan/Packaging) benar.
  2. Stock In dual-mode: toggle ke Material, stock-in Gula Aren 200g @ Rp50 → HPP weighted-average `(500×0+200×50)/700=14.28→14` — toast & data materials cocok persis.
  3. Produk "Kopi Aren" (trackStock:false) + resep via RecipeEditor: picker **hanya menampilkan Material**, bukan Product lagi (celah asli yang dilaporkan user — confirmed fixed). Resep: Gula Aren 20g + Gelas 12oz 1pcs.
  4. Badge stok Kasir: "Stok: 35 pcs" = `min(700/20, 50/1) = min(35,50) = 35` — exact match `getAvailableQty`.
  5. Checkout 3× Kopi Aren → stok material berkurang tepat: Gula Aren 700→640 (−60g), Gelas 50→47 (−3pcs). Profit tercatat Rp 44.160 = `(15000−280)×3` (HPP dari biayaBahan resep, overhead 0 karena `overheadConfig` belum diisi saat itu).
  6. Hapus transaksi + "Kembalikan stok produk" → stok material balik persis ke 700/50 — `applyRecipeAwareStockDelta` arah restore benar.
  7. Overhead Workspace: isi listrik 500rb + peralatan 150rb + penyusutan 38.475, target 1.000 → **`Rp 688` real-time, exact match contoh PLAN.md**. Simpan berhasil.
  8. Jual Kopi Aren (default global, tanpa override) → profit Rp 14.031,525 = `15000−(280+688.475)` — kalkulasi presisi penuh (688.475), bukan versi rounded 688 yang cuma untuk tampilan.
  9. Override fixed 500 pada produk → profit Rp 14.220 = `15000−(280+500)` — override menimpa default global, benar.
  10. Override percent 20% → profit Rp 14.664 = `15000−(280+280×0.2)` — mode percent benar.
  11. Snapshot: transaksi #8/#9/#10 tetap tersimpan dengan profit masing-masing berbeda (tidak dihitung ulang retroaktif saat konfigurasi overhead/produk berubah) — snapshot HPP historis stabil, terverifikasi.
  12. Reports → card "Rekonsiliasi Overhead (Bulan Ini)": Unit Terjual 12, Overhead Terabsorb Rp 8.261,7 (`688.475×12`), Overhead Aktual Rp 688.475, Selisih +Rp 680.213,3 (under-absorbed, merah) — exact match rumus §H.8.
  - **Bug pre-existing ditemukan (di luar scope Fase H, tidak diperbaiki sesi ini):** placeholder field SKU di dialog Tambah/Edit Produk menampilkan raw i18n key `dialog.skuPlaceholder` alih-alih teks placeholder — kemungkinan key hilang dari file locale. Perlu dicek terpisah.
  - **Belum diuji eksplisit** (risiko rendah, jalur kode tidak disentuh Fase H): produk tanpa resep tetap berkurang stok flat 1:1; badge "habis" saat salah satu bahan stoknya 0 (mekanisme sama dengan badge stok normal, hanya beda nilai — sangat mungkin benar berdasarkan hasil test poin 4).
  - **Fase H (H.1–H.16) SELESAI & Acceptance TERVERIFIKASI.** Siap lanjut ke Fase D (pull-sync) — catatan: Fase D butuh backend Fase C/D jalan dulu untuk uji end-to-end (lihat diskusi sebelumnya di sesi ini).
- 2026-08-08 — **Backend Fase A diverifikasi jalan end-to-end.** Setup lokal: `.env` diisi (`VITE_AUTH_API_URL=http://localhost:3210`, `VITE_GOOGLE_CLIENT_ID` dari `.env.example`), Google Cloud Console "Authorized JavaScript origins" diisi `http://localhost:8080` (bukan 5173 — `vite.config.ts` set port custom 8080), redirect URI dikosongkan (flow GSI popup, bukan OAuth redirect classic).
  - Percobaan pertama: login Google sukses tampilkan profil (nama/email dari decode token client-side), TAPI `GET /api/user/profile` & `GET /api/plans` balik **503**, plus `OPTIONS /api/user/profile` **400** (CORS preflight gagal). Diberi requirement debug ke backend agent (cek log server, koneksi DB, `CORS_ORIGINS` harus include `http://localhost:8080`, urutan CORS middleware).
  - Setelah backend difix: `curl http://localhost:3210/api/plans` → 200; CORS preflight dari origin `localhost:8080` → `access-control-allow-origin` benar. Login ulang di browser → **sukses penuh**: profil tampil, plan **"Self-Host Sync" status ACTIVE until 31 Dec 2099** (sesuai desain PLAN.md §2 — self-host selalu ACTIVE far-future), Sync Now/Manage Stores/Web Dashboard/Online Store & Market/Automatic Sync semua kebuka.
  - Device belum di-link ke store ("Device not linked to a store") — itu Fase B (create/select store), belum dikerjakan sesi ini.
  - Dev server ditinggal jalan di `http://localhost:8080` (port 8080, custom, bukan default Vite 5173) via `nvm use 20.20.0`.

## Keputusan penting
- 2026-08-08 — `SyncPayload` (`cloud-api.ts`) bertambah `suppliers?`/`units?`/`paymentMethods?`/`stockIns?`/`stockOuts?`/`hppHistory?` — fix gap, bukan fitur baru (tabelnya sudah dimaksud sinkron sejak `PLAN.md` §3 awal). `reference/cloud-api.ts` & `reference/db.ts` di repo backend sudah di-refresh — backend tinggal implement Fase C/D sesuai kontrak ini apa adanya (6 tabel tsb, `stockIns`/`stockOuts`/`hppHistory` gak punya `isDeleted`/soft-delete, cuma `updatedAt`/`syncedAt`).
- 2026-08-07 — `SyncPayload` (`cloud-api.ts`) bertambah `productRecipes?`/`overheadConfig?` (Fase H). Backend perlu tabel + model Pydantic baru saat siap terima payload ini (lihat Log H.9).
- 2026-08-07 — Revisi Fase H: `ProductRecipe.ingredientProductId` di-rename `ingredientMaterialId` (breaking, clean-cutover, disetujui user). `SyncPayload` bertambah `materials?`. Backend (saat dibangun) perlu tabel `materials` + model Pydantic `Material`, dan field FK di model `ProductRecipe` server disesuaikan namanya.

- 2026-08-08 — **Fix gap push-sync**: `suppliers`, `units`, `paymentMethods`, `stockIns`, `stockOuts`, `hppHistory` sudah punya dirty-tracking hook (`syncTables` di `db.ts`, terpasang lama) TAPI gak pernah di-wire ke `pushSyncData()`/`SyncPayload` — ketinggalan waktu Fase C dikerjakan, ditemukan backend agent saat cek kontrak. Ditambahkan ke `SyncPayload` (`cloud-api.ts`) & `pushSyncData()` (`sync.ts`, 3 titik: `getDirtyRecords`, `totalDirtyCount`, payload push, transaksi update `syncedAt`). `reference/cloud-api.ts` & `reference/db.ts` di `kasirgratisan-BE` di-refresh dari source asli. Real `tsc --noEmit -p tsconfig.app.json` (node 20) → 20 error, identik baseline pre-existing (Onboarding.tsx, use-auth.tsx, db.ts, file-utils.ts, onesignal.ts, Dashboard.tsx, Reports.tsx, 3 file test) — nol regresi. `PLAN.md` §3 frontend gak perlu diubah (list tabelnya sudah benar dari awal, cuma implementasi yang bolong).
  - **Catatan tersisa (di luar scope fix ini, gak disentuh):** `stockOpnameItems` juga gak masuk loop update-`syncedAt` di `pushSyncData()` (beda dari `productRecipes`/dst) — cek lagi kalau sempat.
  - **Untuk backend:** `ingredientMaterialId`/tabel `materials` di `reference/db.ts` backend itu memang benar (hasil revisi Fase H.10–H.16 frontend) — `PLAN.md` backend prosa lama (skema v14) yang stale, bukan salah kontrak. Kontrak asli = `reference/cloud-api.ts` + `reference/db.ts` (barusan di-refresh), bukan teks `PLAN.md`.

- 2026-08-08 — **Fase D (pull-sync client) diimplementasi.** Backend `kasirgratisan-BE` sudah selesai Fase B/C/D (blocker sebelumnya lepas). Ketahuan backend butuh header **`X-Device-Id`** di `POST /sync` (kunci upsert server `(store_id, table_name, device_id, client_id)`) yang **belum pernah dikirim client** — push sebelumnya akan 422 tanpa fix ini (lihat `kasirgratisan-BE/PROGRESS.md` Keputusan). Fix: `syncStoreData(storeId, payload, deviceId)` (`cloud-api.ts`) sekarang wajib kirim `deviceId`, diambil dari `storeSettings.deviceId` di `pushSyncData()`.
  - `cloud-api.ts`: tambah `fetchPullSync(storeId, since?)` → `GET /api/stores/:id/sync?since=` , `SyncPullResponse { serverTime, changes }`.
  - `db.ts`: `StoreSettings.lastPulledAt?: Date | null` (field baru, tanpa bump versi Dexie — bukan indexed field); ditambah ke `sanitizeTableDates`.
  - `sync.ts`: `pullSyncData(storeId)` — baca `lastPulledAt` sebagai `since`, `GET` perubahan, per-tabel bandingkan `updatedAt` lokal vs incoming (skip kalau lokal lebih baru & belum ter-push — melindungi edit offline), konversi field tanggal string→Date manual sebelum `put()` (bukan reuse `sanitizeDatabaseDates()` supaya tidak memicu hook `updating` yang reset `updatedAt`/`syncedAt` jadi dirty lagi), set `syncedAt=serverTime` eksplisit biar record hasil pull tidak ke-push balik sia-sia, simpan `serverTime` sebagai `lastPulledAt` baru — semua dibungkus 1 `db.transaction`. Tabel anak (`transactionItems`/`stockOpnameItems`, tidak punya `updatedAt`) selalu ditimpa langsung (tidak ada dirty-tracking sendiri, sama seperti perilaku push).
  - `triggerBackgroundSync()` sekarang push lalu pull berurutan (memenuhi "startup" & "setelah push" dari PLAN.md §4). `App.tsx` tambah polling ringan `setInterval` 60 detik memanggil `triggerBackgroundSync()` (cleanup di unmount) — memenuhi syarat "polling ringan".
  - Test baru `src/test/sync-pull.test.ts` (3 skenario: merge record baru, lokal lebih baru tidak ditimpa, `since` terkirim dari `lastPulledAt` tersimpan) + `sync-push.test.ts` ditambah assert `deviceId` terkirim. Real `tsc --noEmit -p tsconfig.app.json` (node 20) → 20 error, identik baseline pre-existing (nol regresi). `npx vitest run` → 15/15 pass.
  - **Risiko diketahui, di luar scope fix ini (bawaan desain backend, bukan bug baru):** server tidak me-remap local id Dexie antar device (PK per-device autoincrement) — kunci upsert server pakai `(store_id, table_name, device_id, client_id)` jadi push antar device tidak collide, TAPI hasil pull balik mengandung `id` asli device pengirim; kalau device B kebetulan sudah punya row lokal lain dengan `id` sama di tabel yang sama, `put()` akan menimpa row yang salah. PLAN.md §3 sudah warning soal ini sejak awal ("Catatan konflik ID"); belum ada solusi remapping id di backend maupun client. Risiko nyata untuk toko yang sudah lama dipakai offline di 2 device sebelum sync pertama kali diaktifkan — perlu didiskusikan sebelum rilis produksi kalau skenario ini realistis.

- 2026-08-08 — **Acceptance D diverifikasi end-to-end via browser** lawan backend asli (`kasirgratisan-BE` docker, port 3210 + frontend dev port 8080). Karena tooling browser automation cuma bisa buka tab dalam 1 Chrome profile (IndexedDB dishare, gak bisa simulasi 2 device fisik lewat 2 tab), device B disimulasikan dengan cara sah: relabel sementara `storeSettings.deviceId` device nyata jadi id palsu (`fake-device-b-001`), lakukan perubahan data, push pakai `pushSyncData()` asli (bukan curl/token-extraction — sempat dicoba patch `window.fetch` buat capture header Authorization, **diblokir classifier keamanan karena mirip credential exfiltration, benar & tidak dilawan** — pivot ke cara ini), lalu device id dikembalikan ke aslinya sebelum pull. Login ulang Google diperlukan (token sesi lama sudah expired) — user approve eksplisit di chat sebelum klik "Sign in with Google". Toko baru "Toko OAuth Test" dibuat & ke-bind (`str_e29674bd6f2342df951fc053cd850903`).
  - Skenario 1 (create): kategori "Kategori dari Device B" dibuat & di-push atas nama `fake-device-b-001` → `POST /sync` 200 → dihapus lokal (simulasi "belum ada di device A") → **background auto-sync (debounce 2 detik dari hook `creating`) sudah otomatis pull & muncul kembali sebelum sempat dicek manual** — bukti pull terpicu otomatis via `triggerBackgroundSync()` tanpa aksi user.
  - Skenario 2 (delete/tombstone): kategori sama di-soft-delete (`isDeleted:1`) atas nama device B → push 200 → device id dikembalikan ke device A → `pullSyncData()` manual → `applied:1`, record lokal ter-update `isDeleted:1`/`deletedAt` sesuai server — tombstone propagate benar.
  - Network log konfirmasi round-trip asli: `POST /api/stores/:id/sync` 200, `GET /api/stores/:id/sync?since=...` 200 (bukan mock).
  - **Acceptance D `[x]` TERPENUHI**: perubahan "device lain" muncul di device ini (create), dan hilang (tombstone delete) — keduanya lewat kode produksi asli (`pushSyncData`/`pullSyncData`/`triggerBackgroundSync`), bukan unit test terisolasi.
  - Data uji dibiarkan (kategori id 4, `isDeleted:1`, tidak tampil di UI) — tidak perlu cleanup lebih lanjut, aman diabaikan.

- 2026-08-08 — **Fase E dimulai (bagian mekanis).** User pilih tunda rebrand nama/logo (belum ada aset final) & deploy Vercel dilakukan manual sendiri — scope sesi ini dibatasi ke bagian yang tidak butuh keputusan brand: `version-check.ts` di-no-op (sebelumnya fetch tanpa gate ke `api.kasirgratisan.my.id` bawa `deviceId`+versi — satu-satunya telemetri yang TIDAK sudah di-gate env, beda dari `analytics.ts`/`onesignal.ts` yang sudah aman by-design sejak awal). `vercel.json` baru dibuat (SPA rewrite, belum ada sebelumnya). `.env.example` ditambah catatan `VITE_AUTH_API_URL` produksi. Real `tsc --noEmit -p tsconfig.app.json` (node 20) → 20 error, baseline identik, nol regresi. `npx vitest run` → 15/15 pass.
  - **Tersisa sebelum Acceptance E:** rebrand nama/logo/watermark (nunggu aset dari user), set env var di dashboard Vercel (`VITE_AUTH_API_URL` ke domain backend produksi, `VITE_GOOGLE_CLIENT_ID`, kosongkan GA/OneSignal kalau belum ada akun sendiri), deploy, lalu verifikasi Network tab produksi tidak ada request ke domain pihak asli.

- 2026-08-09 — **User deploy Vercel + backend VPS, Acceptance E diverifikasi.** Frontend: `kasirgratisan-murex.vercel.app`. Backend: `api-kasir.paradox.web.id` (VPS, HTTPS).
  - Percobaan pertama login gagal: `Error 400: invalid_request — Missing required parameter: client_id` dari `accounts.google.com`. Root cause: env var `VITE_GOOGLE_CLIENT_ID` belum di-set di dashboard Vercel (`App.tsx` fallback ke `""` kalau kosong) — bukan bug kode, murni config env yang kelewat saat deploy pertama. User tambah env var + redeploy → fix.
  - Verifikasi otomatis via browser (tab baru, IndexedDB kosong = instalasi baru):
    1. Load `https://kasirgratisan-murex.vercel.app/` → network log 23 request, **nol** request ke `kasirgratisan.my.id`/GA/OneSignal (hanya Google Fonts + accounts.google.com/gsi buat GSI, keduanya legit & perlu).
    2. Onboarding baru → toko "Toko Vercel Test" dibuat lokal.
    3. Login Google sempat gagal diverifikasi via automation (GSI popup buka di window terpisah di luar tab group yang di-track tooling — bukan bug app, keterbatasan tooling browser automation). User login manual sendiri di browser asli → **berhasil**, konfirmasi ke chat.
    4. Reload tab automation (session Chrome sama) → sudah ke-refresh jadi logged-in juga (profil "Yoseph Parai" + "Self-Host Sync until 31 Des 2099" muncul) — konfirmasi `fetchProfile()` cross-domain (Vercel→VPS) sukses.
    5. `Create Store Now` → "Toko Vercel Test" → `POST https://api-kasir.paradox.web.id/api/stores` **200**.
    6. Reload → startup `triggerBackgroundSync()` jalan otomatis → `GET https://api-kasir.paradox.web.id/api/stores/:id/sync` **200** (push di-skip karena belum ada data dirty, benar — bukan kegagalan).
  - **Acceptance E `[x]` TERPENUHI**: app produksi jalan di domain Vercel, tanpa telemetri domain pihak asli (dibuktikan Network tab produksi asli, bukan asumsi), login→store→sync jalan lintas domain Vercel↔VPS.
  - **Belum dikerjakan** (di luar Acceptance E, ditunda sesuai keputusan user 2026-08-08): rebrand nama/logo/watermark, custom domain sendiri (masih pakai domain default `*.vercel.app`).

- 2026-08-12 — **Ad-hoc: fitur invite-link karyawan (di luar Fase A-H).** Ditemukan gap: karyawan gak bisa login di device baru karena `AppLayout.tsx` gate onboarding cuma cek `storeSettings.onboardingDone` lokal (per device) — device kosong selalu jatuh ke wizard `Onboarding`, bukan `LoginScreen`. Satu-satunya jalan bawa data toko ke device baru sebelumnya wajib login Google **owner** (karyawan gak punya akses itu). Ketemu bug tambahan: `pinHash` disalt `deviceId` (random per-device, gak sync) padahal tabel `users` ikut sync — PIN yang dibuat di device A gak valid di device B walau ter-sync.
  - **Fix hash portable:** `User.pinHashV2?` (baru, Dexie v17, no `.upgrade()` — field opsional unindexed) disalt `storeSettings.cloudStoreId` (stabil lintas device, beda dari `deviceId`). `auth.ts`: `createUser`/`updateUserPin` tulis `pinHash` (legacy) + `pinHashV2` (kalau cloud-linked) sekaligus; `login()` cek `pinHashV2` dulu, fallback ke `pinHash`+`deviceId` legacy, dan **self-heal**: backfill `pinHashV2` otomatis saat login legacy sukses di toko yang sudah cloud-linked — tidak perlu migration script.
  - **Refactor `sync.ts`:** ekstrak blok merge `db.transaction` dari `pullSyncData` jadi `applyPullResponse()` reusable, dipakai juga oleh flow join-invite. Pure refactor, no behavior change.
  - **`cloud-api.ts`** tambah 3 fungsi baru: `createStoreInvite(storeId, expiresInMinutes)`, `redeemStoreInvite(token)`, `revokeStoreInvite(storeId, token)` — **kontrak backend baru, BELUM ada implementasinya di backend repo.** Lihat spesifikasi endpoint di bawah.
  - **UI owner** (`Users.tsx`): tombol "Undang via Link" → dialog generate/copy/regenerate link undangan (gate `isOwner && multiUserEnabled && storeSettings.cloudStoreId`).
  - **UI karyawan**: route baru `/join/:token` (`JoinStore.tsx`) — **sibling** dari `<Route element={<AppLayout />}>` di `App.tsx` (bukan nested, biar bisa render di device kosong tanpa kena gate onboarding). Redeem token → `applyPullResponse()` → tulis `storeSettings` lokal (`onboardingDone:true`, `multiUserEnabled:true`, `cloudStoreId`, `deviceId` baru) → redirect `/` → `AppLayout` otomatis jatuh ke `LoginScreen`. Handle device yang sudah pernah setup (`AlertDialog` konfirmasi overwrite) & error state (token invalid/expired/network, tombol retry).
  - i18n lengkap id/en/ms: namespace baru `join.json` (didaftarkan di `src/i18n/index.ts`) + `users.invite.*`/`users.inviteButton` di `settings.json`.
  - Real `tsc --noEmit -p tsconfig.app.json` (node 20) → 20 error, identik baseline pre-existing (nol regresi dari perubahan ini). `npx vitest run` → 15/15 pass.
  - **BLOCKED — butuh kerjaan di repo backend** (ingatkan user salin ke sana + sesuaikan model Pydantic):
    1. `POST /api/stores/{storeId}/invites` — Google auth (punya owner). Body `{ expiresInMinutes?: number }` (default 60). Response `{ token, url, expiresAt }`.
    2. `POST /api/invites/{token}/redeem` — **tanpa** Google auth, rate-limit per token/IP. Response `{ storeId, storeName, serverTime, changes: {...semua SYNC_TABLE_NAMES...} }` (bentuk sama `SyncPullResponse` yang sudah ada). 404 kalau token gak ketemu/revoked, 410 kalau expired (`cloud-api.ts` `JoinStore.tsx` sudah handle kedua status itu spesifik).
    3. `DELETE /api/stores/{storeId}/invites/{token}` — Google auth, buat regenerate/revoke.
  - Belum bisa diverifikasi end-to-end (happy path penuh) sampai backend di atas jalan — UI owner & error-state karyawan sudah testable sekarang via mock.

- 2026-08-12 — **Push ke `main` (`7c05d29`).** Sebelum push: real `tsc --noEmit -p tsconfig.app.json` (node 20) → 20 error, baseline identik. `npx vitest run` → 15/15 pass. Backend ternyata sudah dideploy user ke VPS di antara sesi — smoke test `curl -X POST https://api-kasir.paradox.web.id/api/invites/test-token/redeem` → `404 {"error":"Undangan tidak ditemukan"}`, sesuai kontrak yang diminta (endpoint hidup, error shape benar). `POST /api/stores/:id/invites` & `DELETE .../invites/:token` belum di-smoke-test (butuh Google auth asli, tidak dites lewat curl).
  - **Belum diverifikasi:** flow penuh generate-link (owner) → buka `/join/:token` di device lain → redeem → data ter-pull → login karyawan pakai username/PIN. Perlu dites manual (browser 2 profil/device atau minimal 2 tab beda IndexedDB) sebelum dianggap Acceptance terpenuhi.

## Blocker aktif
- Fitur invite-link karyawan — backend sudah live, **belum ada verifikasi end-to-end** (generate → redeem → login karyawan). Perlu dites manual sebelum di-anggap selesai penuh.
- _(Fase E fungsional selesai & Acceptance lolos. Sisa: rebrand nama/logo kalau/ketika aset final sudah ada, custom domain opsional)_
