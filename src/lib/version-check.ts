// Nonaktif — endpoint asli (api.kasirgratisan.my.id) milik author upstream, bukan domain sendiri.
// Fase E PLAN.md §5: matikan telemetri pihak asli. Arahkan ke domain sendiri di sini kalau
// nanti dibuat endpoint version-check sendiri.
export async function checkVersion(): Promise<void> {
  // no-op
}
